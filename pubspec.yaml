name: splendid_code_quality
description: A Dart CLI tool for analyzing and measuring code quality using industry-standard metrics.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  analyzer: ^6.0.0
  args: ^2.7.0
  pubspec_parse: ^1.3.0

dev_dependencies:
  test: ^1.25.6
  very_good_analysis: ^10.1.0
