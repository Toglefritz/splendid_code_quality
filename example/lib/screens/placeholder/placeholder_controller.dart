import 'package:flutter/material.dart';

import 'placeholder_route.dart';
import 'placeholder_view.dart';

/// Controller for the placeholder screen.
///
/// This controller will be expanded with actual functionality in future updates.
class PlaceholderController extends State<PlaceholderRoute> {
  @override
  Widget build(BuildContext context) => PlaceholderView(this);
}
